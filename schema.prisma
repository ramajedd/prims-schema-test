datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model customers {
  id        String   @id @db.Uuid
  name      String   @db.VarChar(255)
}
